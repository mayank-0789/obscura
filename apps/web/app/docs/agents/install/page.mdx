import { DocArticle } from "@/components/docs/doc-article";

export default function ({ children }) {
  return <DocArticle slug="/docs/agents/install">{children}</DocArticle>;
}

# Install

The `@obscura-app/sdk` package wraps `fetch()` and handles the x402 payment
handshake on 402 responses. Your agent code stays a normal HTTP client —
Payrail signs the on-chain transaction on your behalf when a paid API
asks for payment.

## Requirements

- **Node 18 or later.** The SDK uses the native `fetch` API. On older
  runtimes, inject a polyfill via the `fetch` constructor option.
- **A Payrail account.** [Sign in](/onboarding) with Google or email.
  The signup flow provisions an agent on your first top-up — see
  [Quickstart](/docs/agents/quickstart).

## Install the SDK

<CodeBlock lang="bash" filename="terminal">
{`npm install @obscura-app/sdk

# or pnpm:
pnpm add @obscura-app/sdk

# or yarn:
yarn add @obscura-app/sdk`}
</CodeBlock>

## Verify

A minimal sanity check — this should print the installed version:

<CodeBlock lang="bash" filename="terminal">
{`node -e "console.log(require('@obscura-app/sdk/package.json').version)"`}
</CodeBlock>

You're ready. The [Quickstart](/docs/agents/quickstart) gets you to a
first paid call in about three minutes.
